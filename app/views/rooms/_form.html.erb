<%= simple_form_for @room do |form| %>
  <%= form.input :name %>
  <%= form.label "Module Id" %> <%= form.select :university_module_id, UniversityModule.all.pluck(:id)+ [0] %>
  <%= form.submit "Save", class: 'btn btn-success' %>
<% end %>